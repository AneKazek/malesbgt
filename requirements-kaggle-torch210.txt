# Kaggle-focused dependency set for KCV-TTS with PyTorch 2.10.
# Install this inside the notebook venv after cloning the repo.
# If mamba/causal wheels are unavailable for your exact CUDA+Python combo,
# keep the lines below and let pip build from source, or replace with direct wheel URLs.

# Core torch stack (pin to 2.10)
torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0

# Distributed / training / logging
accelerate>=0.33.0
wandb>=0.19.0
tensorboard>=2.17.0

# Data/audio stack
datasets>=2.21.0
soundfile>=0.12.1
librosa>=0.10.2
cached_path>=1.6.0
hydra-core>=1.3.2
safetensors>=0.4.5
ema-pytorch>=0.5.2
x-transformers>=1.31.14

# Mamba path
causal-conv1d
mamba-ssm

# Helpful extras on Kaggle
ninja
packaging

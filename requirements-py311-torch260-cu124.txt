# Target env: Python 3.11 + PyTorch 2.6.0 CUDA 12.4
# Usage:
#   pip install -r requirements-py311-torch260-cu124.txt
#   pip install causal-conv1d --no-build-isolation
#   pip install mamba-ssm --no-build-isolation

--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu124

# Torch stack
torch==2.6.0
torchaudio==2.6.0
torchvision==0.21.0

# Core training/runtime deps used by this repo
accelerate>=0.33.0
bitsandbytes>0.37.0
cached_path
click
datasets
ema-pytorch>=0.5.2
hydra-core>=1.3.0
librosa
matplotlib
numpy==1.26.4
pydub
pypinyin
rjieba
safetensors
soundfile
tomli
torchcodec
torchdiffeq
tqdm>=4.65.0
transformers
transformers_stream_generator
unidecode
vocos
wandb
x-transformers>=1.31.14

# Build helpers for CUDA extensions
ninja
packaging
setuptools
wheel
